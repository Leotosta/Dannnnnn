import React from 'react';
import { render } from 'react-dom';
import App from './App';
// instancia do app para rederizaçao ( nao se preocupe)
//package.json => servidor para atualizaçao (nao se preocupe)
render(
  
    <App />,
  document.getElementById('root')
);
